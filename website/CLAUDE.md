# CLAUDE.md — KiroCrew dashboard (frontend)

**`website/AGENTS.md` is the single source of truth for frontend rules in
`website/`.** Read it before any UI change; it is imported below so it is always
in context. Do not add frontend rules here — add them to `website/AGENTS.md`.
The backend router is `../AGENTS.md`; **how Claude works** (the loop,
checkpoints, gate ladder, stop points, git autonomy) is the repo-root
`../CLAUDE.md` and applies here unchanged.

@AGENTS.md
