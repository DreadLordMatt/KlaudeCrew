# Progress — issue #21 token display

Spec written 2026-08-19; no implementation slices started yet. This file is
the live resume state per `CLAUDE.md` § 1 — update it before every commit and
whenever a slice finishes, even without a commit.

| Slice | Status | Commit | Gate | Next step |
|---|---|---|---|---|
| 1. Backend: thread token fields through `_attach_turn_stats` | not started | — | — | Read `chat_runner.py:954` and `test/test_turn_stats.py`, then implement |
| 2. Frontend: extend `TurnStats` + render + shared formatter | not started | — | — | — |
| 3. i18n strings (12 locales) | not started | — | — | — |
| 4. Full verify + PR | not started | — | — | — |
